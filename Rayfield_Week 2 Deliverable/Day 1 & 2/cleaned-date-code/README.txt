1. go to https://catalog.data.gov/dataset/high-resolution-floating-solar-pv-data
2. download "Floating PV Windsor CA Data.csv"
3. create a directory called "data" then a directory inside it called "fpv"
4. throw that dataset into "fpv"
5. run pip install -r requirements.txt
7. run read.py and you will see "formatted.csv" get spit out
